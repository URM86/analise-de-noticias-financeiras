# ============================================================
# DEPENDÊNCIAS DO PROJETO: Market Sentiment Monitor
# Monitoramento de sentimento de mercado via Google News RSS
#
# INSTALAÇÃO:
#   1. python -m venv .venv
#   2. .venv\Scripts\activate        (Windows)
#      source .venv/bin/activate     (Linux/macOS)
#   3. pip install -r requirements.txt
# ============================================================

# --- RSS / HTTP ---
feedparser==6.0.12
requests==2.32.3
urllib3==2.3.0

# --- NLP / Análise de Sentimento ---
# torch instalado sem versão fixada pois o índice PyPI
# disponibiliza apenas 2.9+ para Python 3.14 no Windows.
# Para GPU: instale manualmente via https://pytorch.org
torch
transformers>=4.40.0

# --- Tradução (PT-BR → EN antes da análise) ---
deep-translator==1.11.4

# --- Dados / Persistência ---
pandas>=2.2.0
openpyxl==3.1.5

# --- Logging / Utilitários ---
rich>=13.7.0
python-dotenv==1.0.1
schedule==1.2.2
tqdm>=4.66.0